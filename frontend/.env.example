# Leave unset for local dev (requests to /api go through the Vite proxy to localhost:5000).
# In production (Vercel), set this to the deployed backend's URL, e.g.:
# VITE_API_URL=https://api.sufiinfotech.in/api
VITE_API_URL=
